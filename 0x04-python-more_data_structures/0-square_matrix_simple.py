#!/usr/bin/python3
def square_matrix_simple(matrix=[]):
    """
    compute the square of all integers in a matrix
    """
    for i in matrix:
        for j in i:
            k = j**2
    return (k)
